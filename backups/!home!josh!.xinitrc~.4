#!/bin/sh
#

#if [ -d /etc/X11/xinit/xinitrc.d ]; then
#    for f in /etc/X11/xinit/xinitrc.d/*; do
#        [ -x "$f" ] && . "$f"
#    done
#    unset f
#fi

#export GTK2_RC_FILES="$HOME/.gtkrc-2.0"

#exec dbus-launch --exit-with-session openbox-session

xset s off
xset -dpms

env > ~/.env

dbus-launch xfwm4 & wmpid=$!

dbus-launch xfce4-panel &
dbus-launch plank &
dbus-launch urxvtcd &


wait $wmpid
